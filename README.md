# ESP32 2D Minecraft

2D-песочница с процедурной генерацией мира, добычей ресурсов и механикой перемещения, оптимизированная для ESP32 и TFT-дисплея 1.8" (ST7735).

## 🚀 Особенности

* **Процедурный мир:** Карта  блоков с распределением руд по глубине.
* **Движок спрайтов:** Собственные 12 спрайтов 16-битных текстур (земля, камень, трава, 4 вида руды, лестницы и тд).
* **Физика:** Реализована гравитация и проверка коллизий блоков.
* **Система регистров:** Система флагов в регистре каждого блока для отрисовки и взаимодействия с ними.
* **Интерфейс:** Индикатор глубины (Depth Bar).

## 🛠 Аппаратное обеспечение

* **Контроллер:** ESP32 (используется `esp_random()` для генерации мира).
* **Дисплей:** ST7735 160x128 (SPI).
* **Управление:** Аналоговый джойстик (оси X и Y) + кнопки.

### Схема подключения (ST7735)

| Пин дисплея | Пин ESP32 | Описание |
| --- | --- | --- |
| **VCC** | 3.3V | Питание |
| **GND** | GND | Земля |
| **CS** | GPIO 5 | Выбор чипа |
| **RESET** | GPIO 4 | Сброс |
| **D/C** | GPIO 2 | Данные / Команды |
| **MOSI** | GPIO 23 | SPI Data |
| **SCK** | GPIO 18 | SPI Clock |

**Управление:**

* **Joystick X:** GPIO 34
* **Joystick Y:** GPIO 35

---

## 🏗 Структура кода

### 1. Генерация мира (`generateWorld`)

Мир разделен на слои. Шанс появления ценных руд зависит от глубины. Глубже - ценнее руда.

### 2. Структура мира

Мир представляет собой массив с структурами в которых находится 2 байта: 1й-ID спрайта, 2й-регистр состояния/возможных состояний блока.
Структура регистра:
* `0-й бит`: Блок сломан (пустота).
* `3-й бит`: Неразрушимый блок.
* `4-й бит`: Отсутствие коллизии (можно проходить сквозь блок).

### 3. Рендеринг (`render`)

Функция отрисовывает область вокруг игрока в пределах экрана, преобразуя координаты мира в экранные координаты.
> **Важно:** Есть возможность изменения масштабирования (количества отрисованных блоков на экране), но пока не работает с багами.

### 4. Механика движения

При перемещении джойстиком игра проверяет:

1. Является ли блок перед игроком разрушаемым.
2. Если да — "выкапывает" его (меняет тип на фон).
3. После каждого шага вызывается `gravity()`, которая опускает персонажа вниз, если под ним пусто.
4. 
> **Важно:** Реализован бесконечный мир по горизонтали (скроллинг координат `playerX` через `WorldW`).
---

## 🛠 Будущие доработки

* **Система монет:** Получение монет за добычу руд
* **Структуры:** Генерация пещер, гор и тд.
* **Мобы:** Простые ИИ животные с случайными движениями
* **Разнообразие мира:** Добавление воды и физики связанной с ней
* **Оптимизация игры:** С помощью оптимизации хранения массива мира в памяти можно расширить мир или добавить дополнительные спрайты
