# ESP32 2D Minecraft

2D-песочница с процедурной генерацией мира, добычей ресурсов и механикой перемещения, оптимизированная для ESP32 и TFT-дисплея 1.8" (ST7735).

## 🚀 Особенности

* **Процедурный мир:** Карта  блоков с распределением руд по глубине.
* **Движок спрайтов:** Собственные 16-битные текстуры (земля, камень, трава, 4 вида руды, лестницы).
* **Физика:** Реализована гравитация и проверка коллизий блоков.
* **Динамическое освещение (Background):** Система флагов для отрисовки "фоновых" блоков после добычи.
* **Интерфейс:** Отображение координат игрока и индикатор глубины (Depth Bar).

## 🛠 Аппаратное обеспечение

* **Контроллер:** ESP32 (используется `esp_random()` для генерации мира).
* **Дисплей:** ST7735 160x128 (SPI).
* **Управление:** Аналоговый джойстик (оси X и Y) + кнопки.

### Схема подключения (ST7735)

| Пин дисплея | Пин ESP32 | Описание |
| --- | --- | --- |
| **VCC** | 3.3V | Питание |
| **GND** | GND | Земля |
| **CS** | GPIO 5 | Выбор чипа |
| **RESET** | GPIO 4 | Сброс |
| **D/C** | GPIO 2 | Данные / Команды |
| **MOSI** | GPIO 23 | SPI Data |
| **SCK** | GPIO 18 | SPI Clock |

**Управление:**

* **Joystick X:** GPIO 34
* **Joystick Y:** GPIO 35

---

## 🏗 Структура кода

### 1. Генерация мира (`generateWorld`)

Мир разделен на слои. Шанс появления ценных руд () увеличивается с глубиной. Используется побитовая маска для хранения свойств блока:

* `0-й бит`: Блок сломан (пустота).
* `3-й бит`: Неразрушимый блок.
* `4-й бит`: Отсутствие коллизии (можно проходить сквозь блок).

### 2. Рендеринг (`render`)

Функция отрисовывает область вокруг игрока, преобразуя координаты мира в экранные координаты.

> **Важно:** Реализован бесконечный мир по горизонтали (скроллинг координат `playerX` через `WorldW`).

### 3. Механика движения

При перемещении джойстиком игра проверяет:

1. Является ли блок перед игроком разрушаемым.
2. Если да — "выкапывает" его (меняет тип на фон).
3. После каждого шага вызывается `gravity()`, которая опускает персонажа вниз, если под ним пусто.

---

## 📋 Инструкция по сборке

1. Установите библиотеки **Adafruit GFX** и **Adafruit ST7735** в Arduino IDE.
2. Подключите джойстик к указанным пинам (или измените `#define` в начале кода).
3. Прошейте ESP32. При старте в Serial Monitor (115200) можно увидеть процесс генерации.

---

## 🛠 Будущие доработки

* **Инвентарь:** Сбор добытых ресурсов.
* **Крафт:** Использование руды для создания лестниц или инструментов.
* **Мобы:** Простые противники, перемещающиеся по алгоритму случайных блужданий.
